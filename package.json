{
  "name": "staging-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack starter with dual deployment (Cloudflare + VPS)",
  "scripts": {
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:backend": "cd backend && pnpm dev",
    "build:frontend": "cd frontend && pnpm build",
    "build:backend": "cd backend && pnpm build",
    "deploy:cloudflare": "cd frontend && npx @cloudflare/next-on-pages && wrangler pages deploy .cloudflare && cd ../backend && wrangler deploy",
    "deploy:server": "bash server-deploy/deploy.sh"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
